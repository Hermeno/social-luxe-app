import { BottomTabBarProps } from '@react-navigation/bottom-tabs'

// Navigation lives inside ActionBar (right-side pill in the feed viewer)
export default function TabBar(_props: BottomTabBarProps) {
  return null
}
